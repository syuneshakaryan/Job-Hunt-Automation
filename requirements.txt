# =============================================================================
# Job Hunter Pipeline — requirements.txt
# 100% free / local stack
# Install: pip install -r requirements.txt
# Playwright browsers: playwright install chromium
# Ollama: https://ollama.com/download  →  ollama pull llama3.2
# =============================================================================

# ── Orchestration ─────────────────────────────────────────────────────────────
prefect>=3.1.0                  # local flow/task orchestration + UI

# ── Web Scraping & Browser Automation ─────────────────────────────────────────
playwright>=1.44.0              # async chromium automation
beautifulsoup4>=4.12.3          # HTML parsing fallback
lxml>=5.2.2                     # fast HTML/XML parser for bs4
httpx>=0.27.0                   # lightweight async HTTP (for fast HEAD checks)

# ── Local similarity model ──────────────────────────────────────────────────
sentence-transformers>=2.2.2    # semantic similarity scoring with all-MiniLM-L6-v2

# ── Structured Data / Validation ──────────────────────────────────────────────
pydantic>=2.7.1                 # data models & validation
pydantic-settings>=2.3.0        # settings management from .env

# ── Resume Generation ─────────────────────────────────────────────────────────
jinja2>=3.1.4                   # HTML template rendering
weasyprint>=62.3                # HTML → PDF (ATS-clean output)
# Alternative: pdfkit>=1.0.0   # needs wkhtmltopdf system binary

# ── Telegram Integration ──────────────────────────────────────────────────────
python-telegram-bot>=21.3       # async Telegram bot with inline keyboards

# ── Database ──────────────────────────────────────────────────────────────────
# sqlite3 is built into Python — no extra install needed

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1            # load .env config
aiofiles>=23.2.1                # async file I/O
tenacity>=8.3.0                 # retry logic for flaky scrapers
rich>=13.7.1                    # pretty terminal output / logging
tqdm>=4.66.4                    # progress bars for bulk domain processing
python-slugify>=8.0.4           # safe filenames from company names
Pillow>=10.3.0                  # image handling (resume logos etc.)

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.7